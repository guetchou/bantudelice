# Validation 16.5 / 16.6

Date: 2026-03-31
Base verifiee: `https://bantudelice.cg` et `vps-ovh:/opt/bantudelice`

## Correctifs pousses

- `config/sites.php`
  - ajout d'une configuration `ecosystem` branchee via `env(...)`
  - URLs actives:
    - `https://nzila.cg`
    - `https://memela.cg`
    - `https://salisa.cg`
    - `https://kosunga.cg`
- `resources/lang/fr/ui.php`
  - `site.subtitle` passe de `Livraison, mobilité et colis` a `Livraison de repas au Congo`
- `resources/views/frontend/layouts/app-modern.blade.php`
  - `Colis` et `Taxi` retires du premier rang du header
  - regroupes dans un menu secondaire `Services`
  - footer recentre food
- `resources/views/frontend/layouts/app.blade.php`
  - menu legacy recentre
  - `COLIS` et `TAXI` retires du premier rang
  - regroupes dans `AUTRES SERVICES`
- `resources/views/frontend/index-modern.blade.php`
  - dropdown `Nos Plateformes` branche sur l'ecosysteme externe
  - bloc ecosysteme branche sur les 4 URLs externes
  - wording home recentre sur food + ecosysteme

## Verification live

- Home:
  - `200`
  - titre: `BantuDelice — Livraison de repas au Congo`
- URLs ecosysteme presentes dans le HTML live:
  - `https://nzila.cg`
  - `https://memela.cg`
  - `https://salisa.cg`
  - `https://kosunga.cg`
- Chaines residuelles controlees:
  - `Taxi et mobilite` absent
  - `Envoyer un colis` absent
  - `Expedier un colis` absent
  - `Reserver un transport` absent
  - `Taxi` absent comme mot detecte sur la home live
- Routes critiques toujours joignables:
  - `/restaurants`
  - `/track-order`
  - `/livraison-colis`
  - `/suivi-colis`
  - `/transport/taxi`
  - `/cart`
  - `/checkout`
  - `/admin`
  - `/driver`

## Verification navigateur

- Controle Playwright execute le `2026-03-31` sur la home live.
- Rendu confirme:
  - titre `BantuDelice — Livraison de repas au Congo`
  - navigation visible: `Restaurants`, `Offres`, `Nos Plateformes`, `Partenaires`
  - hero CTA: `Commander maintenant`, `Devenir partenaire`
  - bloc ecosysteme visible avec:
    - `Ouvrir Nzila`
    - `Ouvrir Memela`
    - `Ouvrir Salisa`
    - `Ouvrir Kosunga`
  - dropdown `Nos Plateformes` branche sur:
    - `https://nzila.cg/`
    - `https://memela.cg/`
    - `https://salisa.cg/`
    - `https://kosunga.cg/`

## Point utile

- Le probe brut trouve encore `Colis` dans la home live.
- Ce residu provient du contenu ecosysteme/service secondaire et non plus d'un positionnement de premier rang sur le header ni du titre principal.
- Le lien partenaire n'est pas casse:
  - la route nommee `partner` pointe bien vers `/partner/registration`
  - la home live contient `/partner/registration`, pas `/partner`

## Etat

- `16.5`: recentrage public principal valide
- `16.6`: controles techniques principaux valides
- `16.7`: commit non prepare dans ce lot
